/**
 * Feature: history
 *
 * Owns: Phase snapshots, battle history log, decision audit trail,
 *       campaign timeline view.
 * Entities: PhaseSnapshot, BattleCard (read-only), PhaseDecision (locked, read-only)
 * Pages: /campaigns/:id/history
 *
 * Future prompts will add: hooks/useHistory.ts, components/HistoryTimeline.tsx,
 *   components/PhaseSnapshotCard.tsx, etc.
 * Do not add logic to this file yet.
 */
export {};