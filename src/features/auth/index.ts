/**
 * Feature: auth
 *
 * Owns: login, register, password reset, session management, ProtectedRoute logic.
 * Currently handled by: lib/AuthContext.jsx + Base44 SDK auth methods.
 *
 * Future prompts will move AuthContext and auth utilities here.
 * Do not add logic to this file yet.
 */
export {};