/**
 * Feature: campaigns
 *
 * Owns: Campaign creation wizard, campaign joining, lobby management,
 *       campaign settings, player invite flow, campaign status transitions.
 * Entities: Campaign, CampaignPlayer, CampaignInvite
 * Pages: /campaigns/create, /campaigns/join, /campaigns/:id/lobby, /campaigns/:id
 *
 * Future prompts will add: hooks/useCampaign.ts, hooks/useCampaignPlayers.ts,
 *   components/CampaignCard.tsx, services/campaignService.ts, etc.
 * Do not add logic to this file yet.
 */
export {};